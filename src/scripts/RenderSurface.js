

/**
 * RenderSurface
 * A simple abstraction for Pixi so we can swap engines later if need be.
 * @param {Object}     opts        Options
 * @paran {DomElement} opts.parent Parent element to append rendere to.
 */
var RenderSurface = function(opts){
};


module.exports = RenderSurface;